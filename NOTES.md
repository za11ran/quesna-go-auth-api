# Quesna Go — ملاحظات المشروع (لاستمرارية العمل بين الجلسات)

المرجع الكامل للمواصفات: [`BACKEND_HANDOFF.md`](./BACKEND_HANDOFF.md).

## المكوّنات
- **تطبيق Flutter (العميل)** — موجود، **ممنوع تعديله**، مرجع للدراسة فقط.
- **الـ API** (هذا الريبو) — Node/Express + PostgreSQL.
- **الداش بورد (موقع)** — لسه ما اتعملش. أدوار: admin / dispatcher / vendor_owner / vendor_staff / driver.

## قرارات متفق عليها
- الاستضافة: **VPS** (Global VPS، Ubuntu 24، 4GB) — سكربتات في `deploy/`. الدومين `quesnago.com` (هوستنجر). API على `api.quesnago.com`.
- قاعدة البيانات: PostgreSQL على نفس الـ VPS + `deploy/backup.sh` يومي.
- **صور المطاعم/المنتجات: على السيرفر نفسه** (`multer` + `sharp` + `/uploads`) — لسه ما اتعملش.
- الإيميل: **Resend** (`RESEND_API_KEY` في `.env`) — `src/mailer.js` best-effort جاهز.
- اللغة: query `?lang=ar|en` عند التسجيل + هيدر `LANG` في باقي المسارات (كتالوج/طلبات). الرد بيرجّع `user.lang`.
- **تصميم الداش بورد**: يطابق جمالية تطبيق Flutter — أبيض/كروت مدوّرة، لون أساسي أزرق، RTL، مسافات مريحة، بسيط ونضيف. (المستخدم أكّد على ده.)

## المنجز (على `main`)
| المجموعة | الحالة | الملف |
|----------|--------|------|
| Auth (OTP + بروفايل) | ✅ | `routes.auth.js` |
| Customer Catalog (vendors/products/categories/most-requested/offers) | ✅ | `catalog.js` |
| Customer Orders + addresses + notifications + devices | ✅ | `orders.js` |
| Vendor API (login/me/profile/products + **طلبات التاجر** accept/reject/preparing/ready + خصم المخزون) + **Change Requests** | ✅ | `vendor.js` + `changeRequests.js` |
| Admin API (مراجعة CR + approve/reject + إيميل + vendors + most-requested + approval-rules) | ✅ | `admin.js` |
| **Dispatch API** (طابور الجاهز + الدليفري + **rotation** + assign/auto-assign/reassign/unassign) | ✅ | `dispatch.js` |
| **Driver API** (login بالفون + online/offline + ping موقع + accept/reject + انتقالات الحالة + **إشعار العميل** + خصم رصيد + الرسيت) | ✅ | `driver.js` |
| staff auth (admin/dispatcher/driver/vendor) + `orderView.js` مشترك | ✅ | `staff-auth.js` `orderView.js` |

**دورة الطلب الكاملة تعمل end-to-end ومُختبَرة**: عميل يطلب → تاجر يقبل (خصم مخزون) → preparing → ready → مشرف auto-assign بالدور → دليفري يقبل → heading→at_vendor→picked_up (إشعار عميل) → on_the_way (إشعار) → arrived → delivered (إشعار + رسيت + رصيد الدليفري++).

حسابات تجريبية: `admin@quesnago.com/admin1234` · `owner@metro.test/metro1234` · `dispatch1@quesnago.com/disp1234` · driver: phone `+201000000030` / `driver1234`.

## 🚀 الحالة: منشور ومباشر على الإنتاج (2026-09-04)

- **مباشر على `https://api.quesnago.com`** — VPS واحد (`40.160.88.121`, Ubuntu 24) شغّال:
  Node API (PM2 `quesna-api`, port 4000) + PostgreSQL (قاعدة `quesnago`) + Nginx + شهادة Let's Encrypt (تجديد تلقائي).
  - الداش بورد على `/` · الـ API على `/api/` · `/socket.io/` · `/uploads/` — كلها من نفس الدومين بـ HTTPS.
  - كود السيرفر في `/opt/qg` (اتعمله clone من الريبو). التحديث: `cd /opt/qg && bash deploy/update.sh`.
  - `.env` الإنتاج في `/opt/qg/.env` · باسورد قاعدة البيانات في `/opt/qg/deploy/.db_password`.
  - DNS: A record `api` → `40.160.88.121` (هوستنجر).
  - اتأكد فعليًا: `curl https://api.quesnago.com/api/villages` بيرجّع JSON · الداش بورد بتفتح وتسجيل دخول الأدمن شغّال.
- **الباقي (اختياري):** `RESEND_API_KEY`+`ADMIN_EMAIL` في `.env` للإيميلات · crontab لـ `deploy/backup.sh` يوميًا ·
  `openapi.yaml` لسه على auth بس · ربط تطبيق Flutter (`baseUrl = https://api.quesnago.com`).

## ✅ الحالة: الـ API + الداش بورد مكتملين ومختبَرين end-to-end

- **اختبار حقيقي** ضد **PostgreSQL حقيقي (Neon)** = **81/81 ناجح**: auth، الكتالوج، دورة الطلب كاملة
  (عميل → تاجر يقبل ويخصم مخزون → جاهز → المشرف auto-assign بالدور → الدليفري يستلم ويعدّي كل الحالات →
  تم التسليم → إشعارات العميل + الرسيت + رصيد الدليفري)، Change Requests (تاجر→أدمن موافقة/رفض)،
  Admin CRUD (أقسام/بانرات/تجّار/دليفري/مشرفين)، رفع صور (sharp→webp)، Realtime (Socket.IO)، quick-order multipart.
- **الداش بورد** اتجرّب في المتصفح ضد الـ API الحقيقي: دخول ناجح للأدوار الأربعة، التوجيه بالدور، الشاشات بتحمّل بيانات حية.
- **db.sql** صحيح على Postgres حقيقي (اتصلح باگ تكرار الأقسام: dedup + قيد فريد `uq_categories_key`).
- `npm run reset-demo` → يرجّع البيانات التجريبية نظيفة (تجّار 2، منتجات 6، حسابات لوحة 4).
- **النشر**: `deploy/setup-server.sh` بيبني الداش بورد؛ Nginx بيقدّمه على `/` ويمرّر `/api` و `/socket.io` و `/uploads`.
  الداش بورد يستخدم نفس الأصل في الإنتاج (`VITE_API_URL` فاضي).

**الباقي:** ✅ اتنشر على الـ VPS (شوف فوق) · `openapi.yaml` لسه على auth بس (توثيق فقط) · ربط تطبيق Flutter بالـ API.

## منطق الـ API (مكتمل — كل الأدوار + Realtime + العامل الخلفي)

**تفصيل غير المنطقي:**
1. **الداش بورد** — `dashboard/` (Vite + React، RTL). ✅ **كل شاشات كل الأدوار اتعملت**:
   - أدمن: نظرة عامة، طلبات التغيير (diff + موافقة/رفض)، التجّار (+إنشاء متجر)، الأقسام، البانرات، الدليفري، المشرفين، الطلبات.
   - تاجر: طلبات (لحظي)، منتجات (كمية/إخفاء فوري + سعر/أحجام/صورة → CR)، عروض، بروفايل (+لوجو/غلاف/فتح-قفل)، طلبات التغيير.
   - مشرف: طابور التوزيع (auto/manual assign + reassign + unassign، لحظي)، الدليفري + ترتيب الدور.
   - دليفري: طلباتي، online/offline، accept/reject، انتقالات الحالة.
   - `src/socket.js` (Socket.IO client + `useLive` hook).
   - **الباقي**: تجربة end-to-end مع API+DB شغّالين، ربطها بالنشر (تُقدّم من Nginx أو الـ API)، تحسينات UX.
   - تشغيل: `cd dashboard && npm install --ignore-scripts && npm run dev` (5173). البناء لازم يكون في مسار غير الـ sandbox (الـ Desktop تمام).
2. النشر على الـ VPS (السكربتات جاهزة) + Cloudinary اختياري + Resend.
3. ~~`openapi.yaml` — لسه على auth بس~~ ✅ **اتغطّى كل مسارات تطبيق العميل** (auth + كتالوج + طلبات + عناوين/إشعارات/أجهزة) — 27 operationId. مسارات اللوحات (vendor/admin/dispatch/driver) مقصود إنها مش فيه (مش بيستخدمها التطبيق).
4. تكميلي صغير: `/vendor/staff` CRUD، bulk-price، تقارير أعمق.

## تم مؤخرًا
- **زرار تقييم المتجر بقى شغّال فعليًا** — التطبيق كان فيه زرار "قيّم المتجر"
  بيبعت `POST /api/vendors/:id/ratings` من غير ما يكون المسار ده موجود
  بالباك إند خالص، فكل تقييم كان بيفشل (404) والرقم اللي بيظهر كان حساب
  محلي وهمي في التطبيق بس. دلوقتي فيه جدول `vendor_ratings` (تقييم واحد لكل
  عميل لكل متجر، تحديثه بيستبدل القديم)، والمسار بيحدّث `vendors.rating`/
  `reviews_count` فعليًا كمتوسط/عدد حقيقي. الأدمن كمان يقدر يغلب الرقمين
  دول يدويًا من لوحة «التجّار» (مفيد لمتجر جديد لسه ملوش تقييمات كفاية).
  وضفت `clothing`/`stationery` لقايمة `CATEGORY_LABELS` في `catalog.js` اللي
  كانت ناقصة من فيتشر إضافة النوعين دول.
- **موافقة العميل على سعر الطلب السريع** — كان المشرف لما "يقبل" الطلب
  ويحدّد سعر، بيتحوّل status على طول لـ `accepted` وممكن يتعيّنله دليفري من
  غير ما العميل يوافق على السعر أصلًا. دلوقتي فيه حالة وسيطة `price_review`:
  المشرف يحدّد السعر → إشعار فعلي (push) للعميل → العميل يوافق/يرفض
  (`POST /api/orders/:id/quick-price/respond`) → لو وافق يبقى `accepted`
  وقابل للتعيين، لو رفض يتلغي. لوحة التوزيع بتوضّح إنه "بانتظار موافقة العميل"
  وتمنع زرار التعيين لحد ما يوافق.
- **ملحوظة العميل (order.notes) وملحوظة كل صنف (item.note) بقوا ظاهرين** في
  لوحة التاجر والمشرف ولوحة/تطبيق الدليفري — كانوا بيتبعتوا وبيتخزّنوا صح بس
  محدّش كان بيعرضهم في أي واجهة، فكانت بتضيع فعليًا.
- **`address_lat`/`address_lng` بقوا يوصلوا مع الطلب** لو العميل استخدم
  "تحديد على الخريطة" وقت الطلب (مش بس مع عنوان محفوظ من قبل) — والدليفري
  بقى عنده زرار "الملاحة" يفتحله جوجل ماب على الإحداثيات دي مباشرة (تطبيق
  ولوحة الويب).
- **الطلب السريع (`POST /api/orders/quick`) بقى متكامل مع دورة حياة الطلب
  العادي** — كان بيتسجّل في `quick_orders` ويتسيب: مفيش تبليغ للمشرفين
  ومفيش أي مكان يعرضه لغير العميل نفسه، ومكانش بيظهر في "طلباتي" خالص
  (`GET /api/orders` كان بيقرا من `orders` بس). دلوقتي (`src/quickOrderView.js`):
  - بث لحظي + إشعار فعلي لكل مشرف/أدمن وقت الإنشاء (زي أي طلب `ready_for_pickup`).
  - بيظهر في نفس طابور التوزيع (`GET /dispatch/orders`) وفي "طلباتي" (`GET
    /api/orders`) مع الطلبات العادية، وبيستخدم نفس حالات الطلب العادي
    (pending/accepted/assigned/picked_up/on_the_way/delivered/cancelled)
    عشان يشتغل مع شاشة تتبّع الطلب من غير أي تعديل في التطبيق.
  - تعيين الدليفري أبسط من الطلب العادي عمدًا (`POST /dispatch/quick-orders/:id/assign`) —
    مباشر من غير `delivery_offers`/مهلة قبول (لأن الجدول ده مربوط بـ orders
    بمفتاح أجنبي)، ودي مناسبة أكتر لطلب بيتنسّق بمكالمة تليفونية أصلًا.
    الدليفري بيشوفه في صفحته بنفس آلية `driver_sub_status` (`picked_up` وقت
    التعيين → `on_the_way` → `arrived` → `delivered`).
  - المشرف لازم "يراجع ويسعّر" الطلب الأول (`POST /dispatch/quick-orders/:id/accept`
    بسعر حقيقي) قبل ما يقدر يعيّن دليفري — السعر الأصلي مجرد تقدير من العميل.
- **صفحة «الإحصائيات» في لوحة الأدمن** (`/admin/analytics`, `GET /api/admin/analytics`) —
  ثلاث جداول: الدليفري (مرتّبين بـ `deliveries_count` تنازليًا — أكتر حد شغال
  أول واحد)، التجّار (عدد الطلبات/المكتملة/الإيراد عبر `order_vendors`)،
  والمشرفين (عدد الطلبات اللي وزّعوها عبر `orders.dispatcher_id`).
- **وضع الدليفري جوه تطبيق العميل** — بدل ما الدليفري يستخدم لوحة الويب بس،
  دلوقتي فيه زرار «دخول كدليفري» في صفحة البروفايل بتطبيق Flutter (منفصل تمامًا عن
  حساب العميل — توكن مختلف، `DriverApiClient`/`DriverRepo` مستقلين عن `ApiConsumer`
  العادي). بعد الدخول بيشوف نفس اللي في `dashboard/src/pages/driver/Driver.jsx`:
  قايمة طلباته، قبول/رفض التعيين، تقدّم حالة التوصيل، وزرار أونلاين/أوفلاين
  (استطلاع كل ١٠ ثواني بدل Socket.IO — نفس تبسيط تتبّع الطلب).
  - **مسار دخول جديد `POST /api/driver/auth/app-login`** — منفصل عن
    `/auth/login` (اللي لسه بيخدم لوحة الويب من غير أي قيد). محتاج عمود جديد
    `drivers.app_access_enabled` (افتراضي `false`) — الأدمن بيفعّله لكل دليفري
    لوحده من لوحة «الدليفري» (checkbox عمود «دخول من التطبيق»)، عشان يقدر
    يتحكم بالظبط مين يقدر يستخدم الزرار ده بغض النظر عن حساب اللوحة نفسه.
  - **إشعارات Push حقيقية للدليفري** — جدول جديد `driver_devices` (منفصل عن
    `user_devices` لأن الدليفري حساب `drivers`/`staff_users` مش `users`)، وتوكن
    مسار جديد `POST /api/driver/devices` بيسجّله `DriverRepo.registerDeviceToken()`
    بعد الدخول. `sendDriverPush()` في `src/push.js` (نفس helper `sendToTable()`
    اللي بيستخدمه `sendPush()` العادي) بينادى من `assignToDriver()` في
    `dispatch.js` وقت التعيين — منفصل عن `notify()` اللي بيبعت للوحة الويب بس.
  - **جلسة الدليفري بتفضل مفتوحة** — `SplashViewBody` بيتشيّك على
    `DriverRepo.isLoggedIn` قبل أي حاجة تانية وبيروح `/DriverHome` على طول لو
    مفعّلة، حتى لو حساب عميل مسجّل دخول كمان — تفضل مفتوحة لحد ما الدليفري
    يعمل خروج بنفسه من زرار اللوجاوت في صفحته.
  - صفحة الدليفري (`driver_home_view.dart`) اتعمل لها تصميم أشيك: كارت حالة
    بتدرّج لوني ومؤشر أونلاين/أوفلاين، أزرار اتصال مباشر (`url_launcher`) جنب
    التاجر/العميل، Badge «جديد» للتعيينات اللي لسه محتاجة قبول، وتسميات حالة
    مترجَمة بدل القيم الخام (`assigned`/`heading_to_vendor`...).
- **الأقسام (menu_sections) بقت عامة لكل أنواع المتاجر، مش المطاعم بس** —
  لوحة أي تاجر (سوبر ماركت/صيدلية/خضار وفاكهة/مخبز...) فيها دلوقتي صفحة «الأقسام»
  ونماذج المنتجات/العروض بتستخدم نفس آلية `menu_section_id` بدل ليستة
  `SupermarketCategory` الثابتة (اللي كانت خاصة بالسوبر ماركت بس وغير قابلة للتخصيص).
  التطبيق (`vendor_details_view.dart`) بقى بيعرض المنتجات بنفس آلية الأقسام لكل الأنواع
  — شكل شبكة (Grid) للسوبر ماركت والخضار وفاكهة، قائمة طولية لباقي الأنواع، والاتنين
  بيفلتروا بنفس أقسام التاجر المُعرَّفة من لوحته. `SupermarketCategory` القديمة فضلت
  موجودة كحقل تاريخي غير مستخدَم فعليًا في الفلترة تاني.
- **نوع متجر جديد "خضار وفاكهة" (`vegetables`)** — مضاف لقايمة الأنواع في لوحة
  الأدمن (إنشاء متجر) والتطبيق (`VendorType` enum + `CATEGORY_LABELS`). كارت «خضار
  وفاكهة» في الرئيسية كان أصلًا موجود بس **معطوب** (بيفلتر بـ `type=grocery` وده مش
  نوع متجر حقيقي أبدًا — كان بيرجّع نتيجة فاضية دايمًا)؛ اتصلح للنوع الصح.
- **إصلاح: كارت «المزيد» في الرئيسية كان بيرجّع كل المتاجر** (تكرار مع باقي
  الكروت) — بقى بيستثني بالظبط أنواع المتاجر اللي ليها كارت مخصّص أصلًا (مطاعم/
  سوبر ماركت/صيدليات/خضار وفاكهة) عبر باراميتر جديد `GET /api/vendors?exclude_type=`
  (comma-separated، متجاهل لو `type` مبعوت هو الآخر).
- **إشعارات النظام (Push) عبر Firebase شغّالة فعليًا** — `src/push.js` (يستخدم
  `firebase-admin/app`+`firebase-admin/messaging`، الـ API الحديثة الـ modular مش
  `admin.credential.cert()` القديمة اللي في أغلب الأمثلة). `notify()` في `src/notify.js`
  بقى بيبعت push فعلي لكل أجهزة العميل المسجّلة في `user_devices` (بالإضافة للتخزين +
  البث اللحظي اللي كانوا شغّالين أصلًا) — للعملاء بس (`recipientType==='customer'`)،
  مش للوحات (staff_users مالهاش جدول أجهزة). التوكنز اللي بقت غير صالحة (اتشال
  التطبيق...) بتتشال تلقائي من `user_devices` أول ما FCM يرجّع خطأها.
  محتاج `FIREBASE_SERVICE_ACCOUNT_PATH` في `.env` (مسار ملف Service Account سرّي —
  متجاهل في `.gitignore`، مش في الريبو خالص؛ على السيرفر بيترفع يدويًا زي `.db_password`،
  شوف `deploy/DEPLOY.md` §4c). لو المتغيّر مش موجود، السيرفر بيشتغل عادي بدون push.
  تطبيق Flutter اتوصّل من ناحيته كمان (`firebase_core`+`firebase_messaging`+
  `flutter_local_notifications`، `PushNotificationService`) — بيسجّل توكن الجهاز بعد
  تسجيل الدخول وعلى بداية التطبيق لو مسجّل دخول أصلًا، عبر `POST /api/devices` اللي
  كان موجود من زمان بدون استخدام. الـ package name اتغيّر من `com.example.quesna_go`
  الافتراضي لـ `com.quesnago.app` قبل تسجيل Firebase (كان لازم يتعمل قبل أي نشر).
- **اسم المتجر بقى للأدمن بس، حتى مع full_permissions** — `name_ar`/`name_en` اتشالوا
  خالص من `PUT /vendor/profile` (مش موجودين في `allowed` ولا في `approval_rules.vendor_fields`
  تاني) ومن نموذج «بيانات المتجر» في لوحة التاجر. الأدمن بس اللي يقدر يغيّر الاسم
  (`PUT /admin/vendors/:id`) — التاجر يشوفه بس (Read-only) في أعلى الصفحة.
- **إصلاح: أقسام العروض (Category-scope offers) كانت بتتكسر للمطاعم** — عرض بنطاق
  «قسم» بيتطابق مع `products.category`، لكن المطاعم أصلًا مالهاش `category` تاني (بيستخدموا
  `menu_section_id`) — فأي عرض قسم لمطعم كان عمليًا مستحيل يتفعّل. `discountFor()` في
  `catalog.js` و`bestDiscount()` في `orders.js` بقوا بيتطابقوا مع `menu_section_id` كمان.
  ولوحة التاجر: نموذج «عرض جديد» بقى فيه select حقيقي (أقسام قائمة المطعم للمطاعم، أقسام
  السوبر ماركت لغيرهم) بدل حقل نص حر كان سهل يتكتب غلط فيه ومايتطابقش مع حاجة.
- **عروض بنطاق «منتج» بقت بالسعر مباشرة مش بالنسبة** — التاجر يختار المنتج من قايمة،
  السعر الحالي يتعرض تلقائي (Read-only)، وهو يكتب السعر الجديد بس؛ الفرق بيتحوّل تلقائي
  لخصم `amount` ثابت (`discount_value = السعر القديم - الجديد`) — نفس الآلية اللي كانت
  موجودة أصلًا في الباك إند لعروض المنتج المفرد، بس مالهاش واجهة مناسبة قبل كده.
- **الأحجام/الأنواع بقت بتخفي حقل السعر العلوي** — لما التاجر يضيف صف حجم/نوع واحد
  على الأقل في «منتج جديد»/«تعديل منتج»، حقل «السعر» العام بيختفي (مالوش لازمة، كل حجم
  ليه سعره) وسعر المنتج المخزّن بياخده تلقائيًا من أقل سعر بين الأحجام (لعرضه كـ«يبدأ من»).
- **«الأقسام» (أقسام قائمة المطعم) بقت عنصر تنقّل ثابت في قائمة لوحة التاجر** بدل
  ما تكون زرار جوه صفحة «المنتجات» — بيظهر بس للمطاعم (`vendor.type === 'restaurant'`)
  في صفحتَي صاحب المتجر وموظفه.
- **رسوم التوصيل والحد الأدنى للطلب اتشالوا من التاجر خالص** — مالهمش وجود تاني في
  `PUT /vendor/profile` ولا في نماذج الداش بورد (متجر جديد/تعديل متجر/بيانات المتجر).
  رسوم التوصيل بقت بتتحسب من سعر القرية (`deliveryPricing.js`) مش من المتجر أصلًا، ومفيش
  حد أدنى للطلب تاني — تم تصفير `vendors.min_order` لكل المتاجر الموجودة (`db.sql`، وبيانات
  العرض التوضيحي كمان)، وعمود `delivery_fee` فاضل غير مستخدم في الحساب من زمان.
- **صورة المتجر بقت مسؤولية الأدمن بس** — التاجر مالوش أي مسار يغيّر بيه اللوجو/الغلاف
  تاني (اتشال `POST /vendor/profile/logo|cover` خالص، حتى Change Request). بدالها:
  `POST /api/admin/vendors/:id/logo` و`/cover` (فوري، أدمن بس). لوحة الأدمن: مودال
  «تعديل» المتجر فيه رفع لوجو/غلاف؛ وبعد إنشاء متجر جديد بيفتح نفس المودال على طول عشان
  الأدمن يحط الصورة فورًا.
- **مواعيد عمل ابتدائية وقت إنشاء المتجر** — نموذج «متجر جديد» في لوحة الأدمن فيه حقلين
  «من/إلى» (افتراضي 10:00 صباحًا - 2:00 صباحًا لكل الأسبوع)، `POST /api/admin/vendors`
  بيبنيهم لشكل `working_hours` الكامل. بعد الإنشاء، التاجر يقدر يعدّل مواعيده براحته زي
  ما كان (`PUT /vendor/profile/working-hours` — فوري، من غير موافقة، ما اتغيّرش).
- **إصلاح: لوحة التاجر كانت بتعرض أقسام السوبر ماركت (grocery/dairy/...) في نموذج
  «منتج جديد» و«تعديل منتج» لكل التجّار بما فيهم المطاعم**. دلوقتي لو المتجر مطعم
  (`vendor.type === 'restaurant'`)، نموذج «منتج جديد» بيعرض **قسم القائمة** (بيتزا/برجر...)
  بدل قائمة أقسام السوبر ماركت، والقسم دا بيتبعت مع الإنشاء (`menu_section_id` — عمود جديد
  في `newValues` بمسار `POST /vendor/products`، فوري زي أي منتج تاني). نموذج «تعديل منتج»
  بيخفي حقل «القسم» للمطاعم خالص (مفيش استخدام له عندهم أصلًا — قسم القائمة بيتغيّر من عمود
  الجدول مباشرة). المتاجر التانية (سوبر ماركت/صيدلية/مخبز) لسه بتشوف أقسام السوبر ماركت
  زي ما هي.
- **إضافة: الأحجام/الأنواع (Options) بقت متاحة في نموذج «منتج جديد» مش بس «تعديل منتج»** —
  كان التاجر مضطر يعمل المنتج الأول وبعدين يدخل يعدّله عشان يضيف أحجام. `POST /vendor/products`
  بقى بيقبل `options[]` (زي `PUT`)، ويطبّقها فورًا لو `full_permissions`، أو يحفظها جوه
  الـ Change Request عادي وتتطبّق وقت الموافقة (`applyChangeRequest`'s `create` branch كان
  بيتجاهل `options` تمامًا — باگ منفصل اتصلح كمان).
- **صلاحيات التاجر (Change Request) — تخفيف عام + علامة "صلاحيات كاملة" لكل متجر**:
  - **فوري لكل التجّار دايمًا** (اتشال من نظام الموافقة خالص): سعر المنتج (`PUT /vendor/products/:id`
    بيطبّقه فورًا حتى لو باقي الحقول محتاجة موافقة)، الكمية/التوفر (كانت فورية أصلًا)،
    مواعيد الفتح/الغلق (كانت فورية أصلًا)، والعروض بالكامل (إنشاء/تعديل/حذف — كانت كلها
    Change Request إجباري، بقت `INSERT/UPDATE/DELETE` مباشر في `src/vendor.js`).
  - **عمود جديد `vendors.full_permissions`** (افتراضي `false`) — لما الأدمن يفعّله لمتجر
    معيّن (لوحة «التجّار» → عمود «الصلاحيات») بيبقى كل تعديلاته فورية بالكامل (اسم/وصف/
    لوجو/غلاف/صورة منتج/منتج جديد/حذف منتج/أحجام) من غير أي Change Request — قابل للتبديل
    في أي وقت (`PUT /api/admin/vendors/:id/full-permissions`). المتاجر العادية (`false`)
    فاضلة بنفس نظام الموافقة القديم للحقول دي.
  - `hasFullPermissions(vendorId)` في `src/changeRequests.js` هي نقطة التحقق المشتركة،
    مُستخدمة في كل مسارات `vendor.js` الحسّاسة.
- **الأدمن يقدر يعدّل اسم/بيانات أي متجر مباشرة** — `PUT /api/admin/vendors/:id` (اسم عربي/
  إنجليزي، وصف، تليفون، رسوم توصيل، حد أدنى) بيطبّق فورًا بدون Change Request (الأدمن أصلًا
  هو المُوافِق). لوحة «التجّار» فيها زرار «تعديل» لكل متجر.
- **الأدمن يقدر يغيّر اسم/باسورد أي حساب لوحة (أدمن/مشرف/دليفري/صاحب متجر/موظف متجر)** —
  مسار عام جديد `PUT /api/admin/staff/:id` (بديل `staff_users.name`/`password_hash`؛
  لو الحساب دليفري بيحدّث نسخة `drivers.name/phone` كمان عشان تفضل متطابقة). في اللوحة:
  زرار «تعديل» في صفحتَي «الدليفري»/«المشرفين»، وقايمة حسابات دخول المتجر (صاحبه +
  موظفينه) جوه مودال تعديل المتجر — `GET /api/admin/vendors/:id/staff`.
- **متاجر "يدوي" (بدون تطبيق تاجر) — الأدمن بيتحكم لكل متجر** — عمود جديد
  `vendors.order_mode` (`app` الافتراضي، أو `manual`). لوحة الأدمن: صفحة **«التجّار»**
  فيها select لكل متجر لتغيير النوع (`PUT /api/admin/vendors/:id/order-mode`).
  لما كل متاجر السلة `manual`، `POST /api/orders` بيتخطى خطوة "قبول التاجر" بالكامل:
  المخزون بيتخصم فورًا، والطلب بيتحوّل مباشرة لـ `ready_for_pickup` (بدل ما يفضل `pending`
  لحد ما حد يقبله) ويظهر على طول في **طابور التوزيع** — مع رسالة تنبيه "📞 اتصل بـ [المتجر]"
  (اسم + تليفون المتجر، متلقّطين وقت الطلب في `order_vendors.order_mode`/`vendor_phone`).
  المشرف بيتصل بالمطعم تليفونيًا لتأكيد الطلب، وبعدين بيعيّن الدليفري زي أي طلب عادي
  (auto/manual assign من نفس الطابور) — والدليفري بعد كده بيحدّث حالة الطلب من تطبيقه
  زي المعتاد (مفيش تغيير في تدفّق الدليفري). لو السلة فيها متجر `app` واحد على الأقل مع
  متجر `manual`، الطلب بيمشي بالتدفّق العادي (التاجر اللي عنده تطبيق هو اللي بيقبل) —
  الحالة دي (سلة مختلطة) مش متغطّاة بالكامل لسه، نادرة الحدوث حاليًا.
- **تسعير التوصيل حقيقي من لوحة الأدمن** (بدل مجموع رسوم كل متجر) — `src/deliveryPricing.js`:
  إجمالي توصيل الطلب = `villages.delivery_base_fee` (سعر أساسي حسب قرية العميل، عمود جديد) +
  `app_settings['delivery_pricing'].extra_vendor_fee` (رسوم ثابتة لكل متجر إضافي في نفس الطلب،
  كانت 15 ج.م مضروبة في تطبيق Flutter، دلوقتي من السيرفر). `POST /api/orders` بيحسبها فعليًا كده.
  لوحة الأدمن: صفحة **«أسعار التوصيل»** (`/admin/delivery-pricing`) — تعديل الرسوم العامة
  (`GET/POST /api/admin/settings/delivery-pricing`) + تعديل سعر كل قرية على حدة
  (`GET /api/admin/villages`, `PUT /api/admin/villages/:id`). `GET /api/villages` العام
  بقى بيرجّع `delivery_base_fee` لكل قرية و`extra_vendor_fee` عام — التطبيق لازم يستخدمهم
  بدل القيم الثابتة المحلية في `ShippingPricing` عشان معاينة السلة تطابق فعليًا اللي هيتحسب وقت الطلب.
  عمود `vendors.delivery_fee` القديم فاضل في الجدول بس مش بيُستخدم في حساب إجمالي العميل تاني.
- **رفع صورة البروفايل للعميل** — `POST /api/auth/me/avatar` (multipart، حقل `image`) فوري بدون Change
  Request (صورة العميل نفسه مش صورة متجر). `PATCH /api/auth/me` أصلاً بيدعم `email`/`birth_date`/`gender`.

- **أكواد الخصم (Coupons) حقيقية** — جدول `coupons` (الأدمن هو اللي بيكتب الكود من لوحته). عام: `POST /api/coupons/validate` (معاينة الخصم في السلة قبل الطلب). `POST /api/orders` بيقبل `coupon_code` اختياري، بيتحقق منه تاني، بيطبّق الخصم على `discount_total`/`total`، وبيزوّد `used_count` — كله جوه نفس الـ transaction. لوحة الأدمن: صفحة «أكواد الخصم» (CRUD كامل: نسبة/مبلغ ثابت، حد أدنى للطلب، أقصى عدد استخدام، فترة صلاحية).

- **أقسام قائمة المطعم (Menu Sections)** — كانت ناقصة (اكتشفناها من مراجعة كود تطبيق Flutter اللي كان مستنيها). جدول `menu_sections` (لكل تاجر) + `products.menu_section_id`. عام: `GET /api/vendors/:id/menu-sections`. لوحة التاجر: `GET/POST/PUT/DELETE /api/vendor/menu-sections[/:id]` (فوري، بدون Change Request) + تعديل فوري لقسم أي منتج عبر `PATCH /vendor/products/:id { menu_section_id }`. الداش بورد: صفحة `MenuSections.jsx` تظهر لصاحب مطعم بس (زر من صفحة المنتجات).

- **مراجعة اللغة (ar/en) في كل مسارات العميل**: `src/lang.js` موحّد — `langOf(req)` بيقرأ هيدر `LANG` ثم `?lang=` ثم `body.lang` ثم `ar`. الكتالوج والطلبات بيستخدموه (الطلبات كانت بتقرأ الهيدر بس، دلوقتي بتقرأ الباراميتر كمان). إشعارات العميل بقت ثنائية اللغة `{ ar, en }` و`notify()` بيختار حسب `users.preferred_language` للعميل (مش لغة طلب الدليفري/التاجر). أُضيف إشعار **«طلبك جاهز»** عند `ready_for_pickup`. أخطاء الـ API عربي فقط **بالتصميم** — التطبيق بيترجم `error_code` بنفسه.
- **الداش بورد**: صفحة **«الأكثر طلبًا»** للأدمن (`/admin/most-requested`) — تحديد منتجات قسم الأكثر طلبًا في التطبيق. مدعومة بـ `GET /api/admin/products` (قائمة كل المنتجات + اسم المتجر + `is_most_requested`).
- **`API_GUIDE.md`**: دليل ربط تطبيق Flutter بالـ API (Base URL، التوكن، هيدر LANG، كل مسارات العميل، Socket.IO، أمثلة Dart).
- **رفع الصور**: `src/upload.js` (multer memory + sharp → webp، 5MB، jpg/png/webp) → `/uploads` (Nginx مباشرة). لوجو/غلاف/صورة منتج → **Change Request**. `/api/admin/categories|banners|... /image` و `/api/orders/quick` (multipart، حتى 5 صور).
- **Vendor**: `PUT /vendor/profile/working-hours` (فوري)، **عروض CRUD** كلها Change Request؛ `applyChangeRequest` يدعم `entity_type='offer'`.
- **Admin**: أقسام الهوم CRUD، البانرات CRUD، **إنشاء متجر + حساب صاحبه**، **حسابات دليفري/مشرفين**، `/admin/orders` `/admin/users` `/admin/reports`. + `GET /api/home/banners` للعميل.
- **Realtime** (`src/realtime.js`, Socket.IO على `/socket.io`، JWT في `handshake.auth.token`): غرف `customer:<id>` `staff:<id>` `vendor:<id>` `driver:<id>` `role:<role>`. أحداث: `notification:new` (كل إشعار)، `order:new` (للتاجر)، `order:update` (للعميل مع كل تغيّر حالة)، `dispatch:needs_assignment` (للمشرف)، `driver:assignment` (للدليفري). Nginx بيمرّر WebSocket.
- **العامل الخلفي** (`src/worker.js`): كل 20 ثانية يلغي عروض التوصيل اللي عدّت المهلة، يحرّر الدليفري، يرجّع الطلب `ready_for_pickup`، ويبثّ `dispatch:needs_assignment`. يتعطّل بـ `DISABLE_WORKER=1`.

## اختبار محلي
`pg-mem` غير مثبت كـ dependency — تُستخدم مؤقتًا في سكربتات الاختبار.
تحذير: تجنّب المنفذ 4190 في أي test (منفذ محظور في `fetch`).
